package org.mihajlo1612.showtime

import androidx.compose.ui.graphics.Color

object ShowtimeColors {

    // Background
    val BackgroundPage = Color(0xFF0A0A0F)
    val BackgroundCard = Color(0x85121126)

    // Primary (Red)
    val PrimaryRed = Color(0xFFE50914)
    val PrimaryRedDark = Color(0xFFC0070F)
    val PrimaryRedDeep = Color(0xFFB00710)
    val AccentOrange = Color(0xFFFF6B35)

    // Text
    val TextPrimary = Color(0xFFFFFFFF)
    val TextSubheading = Color(0x66FFFFFF)
    val TextLabel = Color(0x80FFFFFF)
    val TextRegister = Color(0x61FFFFFF)
    val TextPlaceholder = Color(0x33FFFFFF)

    // Input
    val InputBackground = Color(0x0DFFFFFF)
    val InputBorder = Color(0x17FFFFFF)
    val InputBorderFocused = Color(0x80E50914)
    val InputBackgroundFocused = Color(0x0AE50914)
    val InputFocusRing = Color(0x1AE50914)
    val InputIcon = Color(0x40FFFFFF)

    // Card
    val CardBorder = Color(0x12FFFFFF)

    // Shadow / Glow
    val ShadowRed = Color(0x66E50914)
    val ShadowRedHover = Color(0x8CE50914)
}